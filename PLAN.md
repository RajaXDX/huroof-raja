# حروف مع رجا — خطة التطوير وحالة المشروع

> **لأي مساعد ذكاء اصطناعي يقرأ هذا الملف:** هذا هو المرجع الرسمي لحالة المشروع.
> اقرأه كاملاً قبل البدء، ثم **حدّث قائمة المهام في الأسفل** كلما أُنجز بند
> (`[ ]` ← `[x]` مع تاريخ ورقم الـ commit). آخر تحديث: **2026-07-31**.

---

## 1. ما هو المشروع

لعبة **خلية الحروف**. لوحة مسدسات، في كل مسدس حرف وسؤال إجابته تبدأ بذلك الحرف.
فريق يصل بين أعلى اللوحة وأسفلها، والآخر بين يمينها ويسارها.

| | |
|---|---|
| **التقنية** | HTML + CSS + JavaScript خام — بدون أي مكتبة أو أداة بناء |
| **الوضع** | محلي فقط (جهاز واحد، فريقان يتناوبان) |
| **المستودع** | `github.com/RajaXDX/huroof-raja` (فرع `main`) |
| **اللغة** | عربي RTL بالكامل |
| **المشروع الشقيق** | «تحدي رجا» — نفس الهوية اللونية، مشروع منفصل تماماً |

---

## 2. القرارات المعمارية المتّخذة (2026-07-31)

هذه قرارات المستخدم صراحةً — لا تُغيَّر بلا طلب:

1. **مشروع ومستودع منفصلان** عن «تحدي رجا»، لا مجلد داخله ولا مود إضافي
2. **محلي فقط** — لا Supabase ولا رومات ولا حسابات
3. **الإجابة الخاطئة تنقل الخلية للفريق الآخر مباشرة** (القاعدتان الأخريان
   موجودتان كخيار في الإعدادات لكن هذه الافتراضية)
4. **بنك أسئلة كامل من البداية** — 280 سؤالاً جاهزة

---

## 3. الحالة الحالية — ما أُنجز ✅

### البنية
- ✅ خمسة ملفات JS بفصل صارم: `hive.js` منطق خالص، `game.js` قرارات،
  `ui.js` رسم، `setup.js` إعدادات، `utils.js` أدوات
- ✅ خمس شاشات: الرئيسية · كيف نلعب · الإعدادات · اللعب · النهاية
- ✅ الهوية مبنية على `واجهة لعبة تفاعلية/design_handoff_horoof_game`:
  **ستة ثيمات** في `js/themes.js`، خطوط Cairo + Almarai محزومة محلياً

### اللعب
- ✅ لوحة مسدسات بصفوف مُزاحة، الحجم ٤×٤ إلى ٧×٧
- ✅ **ترتيب الحروف عشوائي في كل جولة**
- ✅ كشف الفوز بـ BFS على إحداثيات محورية + إبراز مسار الفوز متحركاً
- ✅ أشرطة ملوّنة على الحواف الأربع تبيّن اتجاه كل فريق
- ✅ نافذة السؤال: الحرف، السؤال، تلميح، مؤقّت، كشف الإجابة، حكم صح/خطأ
- ✅ ثلاث قواعد للإجابة الخاطئة
- ✅ جولات متعددة مع إنهاء مبكر عند حسم الأغلبية
- ✅ **تبديل الاتجاهات كل جولة** لإلغاء أفضلية الاتجاه العمودي
- ✅ من يبدأ: قرعة في الجولة الأولى، ثم يبدأ المتأخّر في النتيجة
- ✅ اختصارات لوحة مفاتيح للمقدّم (مسافة / 1 / 2)
- ✅ مؤثرات صوتية مولّدة برمجياً (بلا ملفات صوت)

### الإعدادات
- ✅ **اختيار الثيم** من ستة، وتبديله يعيد ألوان الفريقين لافتراضياته
- ✅ أسماء الفريقين وألوانهما (8 ألوان لكل ثيم، ومنع اللونين المتطابقين)
- ✅ حجم الخلية، عدد الجولات، وقت السؤال، قاعدة الخطأ
- ✅ معاينة مصغّرة حيّة تُظهر أثر الحجم واللون قبل البدء
- ✅ الإعدادات تُحفظ في `localStorage` وتبقى بين الجلسات

### بنك الأسئلة
- ✅ **280 سؤالاً، 10 لكل حرف من الـ28**
- ✅ متحقَّق آلياً: كل إجابة تبدأ بحرفها فعلاً، ولا سؤال مكرر
- ✅ لا يتكرر السؤال داخل المباراة الواحدة حتى تنفد أسئلة الحرف

---

## 4. ما جرى التحقق منه فعلياً (لا قراءة كود فقط)

| الاختبار | النتيجة |
|---|---|
| 200,000 لوحة عشوائية، مقاسات ٤–٧ | **صفر تعادل، صفر فوز مزدوج** |
| 540 مباراة كاملة عبر دوال اللعبة الحقيقية | **صفر مسار فوز خاطئ، صفر تعليق** |
| صحة كل مسار فوز (متّصل + يلمس الحافتين) | ✅ بمدقّق مستقل |
| مباراة ٣ جولات كاملة | ✅ التبديل والأغلبية والشاشة النهائية |
| نافذة السؤال والمؤقت | ✅ العرض والحكم والإيقاف |
| الجوال 375×812، كل المقاسات | ✅ بلا تجاوز أفقي، اللوحة كاملة بلا تمرير |
| الشاشة الكبيرة، كل المقاسات | ✅ |
| 280 إجابة مقابل حروفها | ✅ كلها مطابقة، صفر تكرار |
| الثيمات الستة × 24 متغيّراً | ✅ كلها تُحَل، وألوان الفرق تتبع الثيم |
| 150 مباراة بعد إعادة بناء الواجهة | ✅ 7,606 سؤال، صفر أخطاء |
| مقاس الخلية مقابل المواصفة على 1440×960 | ✅ 106 / 94 / 82 / 72 مطابقة |
| اللعبة والخادم مطفأ (تحقق بـ curl) | ✅ مباراة كاملة من المخزن |

**التعادلات الوحيدة** ظهرت في قاعدة «الخلية تُقفل» (26 من 180)، وهي متوقعة
لأن القفل يحذف خلايا فتنكسر الضمانة الرياضية. اللعبة تتعامل معها بالفوز
لصاحب الخلايا الأكثر.

---

## 5. أشياء مهمة يجب معرفتها قبل التعديل ⚠️

1. **`hive.js` لا يحوي أي تعامل مع RTL** وهذا مقصود. «من اليمين لليسار» و«من
   اليسار لليمين» شرط واحد رياضياً. لا تُدخل اتجاه العرض في منطق اللعبة.

2. **الإحداثيات محورية داخلياً** رغم أن العرض بصفوف مُزاحة. لا تحسب الجيران
   في الإحداثيات المُزاحة — تختلف حسب فردية الصف وهو مصدر أخطاء كلاسيكي.

3. **متغيرات الحالة `let` في نطاق السكربت** (`SETTINGS`, `BANK`, `Game`).
   `window.SETTINGS = x` لا يغيّرها. للاختبار من الكونسول استخدم `eval` غير
   المباشر: `const ev = eval; ev("SETTINGS.size = 6");`

4. **لا تُخمّن المساحة برقم ثابت في `computeHexWidth`.** الثوابت المخمَّنة
   سبّبت مرتين مشاكل حقيقية: تجاوزاً أفقياً، ثم أكثر من 100px فاضية أسفل
   اللوحة على الجوال. المساحة تُقاس الآن من التنسيق المطبَّق ومن موضع
   الحاوية وارتفاع التذييل الحقيقي.

4.1 **ارتفاع بطاقات الفرق يحكم حجم اللوحة.** حين كُدّس محتواها عمودياً
   ارتفعت إلى 154px فانكمشت الخلية من 94 إلى 48. أبقِها سطرين.

5. **ارفع `CACHE_VERSION` في `sw.js` بعد أي نشر.** عامل الخدمة يخزّن
   cache-first ويتجاهل `?v=` (بسبب `ignoreSearch: true`)، فبلا رفع الرقم
   يبقى اللاعبون على النسخة القديمة إلى الأبد. هذا عطّل التطوير مرتين:
   تعديلات مطبَّقة على القرص ولا تظهر في المتصفح.

6. **الاختبار**: `npm test` يشغّل `tools/test.js` في متصفح نظيف بلا كاش.
   استخدمه بدل الكونسول اليدوي — المتصفح يخبّئ الملفات فتُقرأ نسخة قديمة
   ويبدو التعديل بلا أثر. الطريقة اليدوية القديمة:
   ```bash
   python -m http.server 8210 --bind 127.0.0.1
   ```
   ثم التحقق من المتصفح بـ JavaScript (تشغيل مباريات كاملة برمجياً وقياس
   العناصر). **لا تكتفِ بقراءة الكود.**

7. **لا يوجد اختصار لإغلاق نافذة السؤال** عمداً — الإغلاق بلا حكم تهرّب.

8. **رسائل الـ commit بالإنجليزية**، وتذكر ما جرى التحقق منه فعلياً.

---

## 6. قائمة المهام 📋

### 🔴 قبل أول جلسة حقيقية

- [ ] **جلسة لعب بشرية كاملة** — كل الاختبارات حتى الآن آلية. لم يلعبها إنسان
  من البداية للنهاية. هذه أهم من أي ميزة إضافية.

### 🟠 مهم

- [ ] **توسيع بنك الأسئلة** — 10 أسئلة لكل حرف تكفي لمباراتين أو ثلاث قبل أن
  تبدأ الإعادة. الهدف المعقول 25–30 لكل حرف (≈800 سؤال).
- [x] **مستوى الأسئلة** — أُنجز 2026-07-31، البنك كله صار مستوى **متوسط**
  (`_meta.difficulty`). تم التحقق: 280 سؤالاً، 28 حرفاً، صفر إجابة لا تبدأ
  بحرفها، صفر تكرار.
- [ ] **تقسيم لمستويات** — سهل/متوسط/صعب يسمح
  بلعب العائلة والأطفال معاً.
- [x] **تبديل السؤال غير المُجاب** — أُنجز 2026-07-31. كل خلية تبقى متاحة
  بعد فتح سؤالها تأخذ سؤالاً جديداً (إلغاء، خطأ مع «تبقى متاحة»، انتهاء وقت).
- [ ] **زر «استبدل السؤال»** للمقدّم قبل الإجابة، حين يكون السؤال غير مناسب.

### 🟡 تحسينات ظاهرة

- [x] **شعار وأيقونة** — أُنجز 2026-07-31. `tools/make-icons.py` يولّد 11 مقاساً
  وشاشة بداية من تعريف واحد، ووسوم `og:` مضافة.
- [x] **تطبيق مواصفة التصميم** — أُنجز 2026-07-31. الواجهة أُعيد بناؤها على
  `واجهة لعبة تفاعلية/design_handoff_horoof_game`: ستة ثيمات، خطوط
  Cairo + Almarai، شريط تنقّل، وإطار أشرطة حول اللوحة.
- [x] **عمل بلا إنترنت** — أُنجز 2026-07-31. الخطوط محزومة محلياً و`sw.js`
  يخزّن كل شيء. تم التحقق والخادم مطفأ: مباراة كاملة لُعبت.
- [x] **رفع على GitHub** — أُنجز 2026-07-31، مستودع عام `RajaXDX/huroof-raja`
- [ ] **تفعيل GitHub Pages** — خطوة يدوية في Settings → Pages
  (Deploy from a branch · `main` · `/ (root)`). الرابط بعدها:
  `rajaxdx.github.io/huroof-raja`
- [x] **تجهيز App Store** — أُنجز 2026-07-31. مشروع Capacitor iOS، سير
  GitHub Actions يبني على ماك سحابي (صاحب المشروع على ويندوز)، سياسة
  خصوصية، ونصوص المتجر. الدليل في `APPSTORE.md`.
  ⚠️ **لم يُبنَ فعلياً بعد** — يحتاج تشغيل السير على ماك. البناء بلا توقيع
  متاح مجاناً ويجب تجربته قبل دفع الـ99 دولاراً.
- [ ] **صوت أفضل** — النغمات المولّدة وظيفية لكنها بسيطة
- [ ] **حفظ نتائج المباريات** محلياً (كم مباراة، من فاز أكثر)

### 🟢 لاحقاً

- [ ] **وضع أونلاين** — الفصل بين `game.js` و`ui.js` مُعدّ لهذا: `Game`
  هو ما يُزامَن. لكن القرار الحالي محلي فقط.
- [ ] **وسائل مساعدة** مثل «تخطّي» أو «سؤال بديل» بحصة محدودة لكل فريق
- [ ] **أكثر من فريقين**

---

## 7. كيف تُحدَّث هذه الخطة

عند إنجاز أي بند:
1. غيّر `[ ]` إلى `[x]` وأضف: `— أُنجز 2026-XX-XX (commit abc1234)`
2. انقل ما استُجدّ من ملاحظات إلى القسم 3 أو 5
3. غيّر تاريخ «آخر تحديث» في أعلى الملف
4. أضف أي مهمة جديدة في مكانها حسب الأولوية
