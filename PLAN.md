# حروف مع رجا — خطة التطوير وحالة المشروع

> **لأي مساعد ذكاء اصطناعي يقرأ هذا الملف:** هذا هو المرجع الرسمي لحالة المشروع.
> اقرأه كاملاً قبل البدء، ثم **حدّث قائمة المهام في الأسفل** كلما أُنجز بند
> (`[ ]` ← `[x]` مع تاريخ ورقم الـ commit). آخر تحديث: **2026-08-14**.

---

## 1. ما هو المشروع

لعبة **خلية الحروف**. لوحة مسدسات، في كل مسدس حرف وسؤال إجابته تبدأ بذلك الحرف.
فريق يصل بين أعلى اللوحة وأسفلها، والآخر بين يمينها ويسارها.

| | |
|---|---|
| **التقنية** | HTML + CSS + JavaScript خام — بدون أي مكتبة أو أداة بناء |
| **الوضع** | **محلي** (جهاز واحد) + **أونلاين** (روم بكود، حتى 8 لاعبين) |
| **المستودع** | `github.com/RajaXDX/huroof-raja` (فرع `main`) |
| **اللغة** | عربي RTL بالكامل |
| **المشروع الشقيق** | «تحدي رجا» — نفس الهوية اللونية، مشروع منفصل تماماً |

---

## 2. القرارات المعمارية المتّخذة (2026-07-31)

هذه قرارات المستخدم صراحةً — لا تُغيَّر بلا طلب:

1. **مشروع ومستودع منفصلان** عن «تحدي رجا»، لا مجلد داخله ولا مود إضافي
2. ~~**محلي فقط** — لا Supabase ولا رومات ولا حسابات~~
   **نُقض بطلب المستخدم صراحةً (2026-08-07):** أُضيف وضع أونلاين برومات
   على Supabase. و**الوضع المحلي باقٍ كما هو** — قرار صريح أيضاً، لأن
   `APPSTORE.md` يسوّق اللعبة على أنها تعمل بلا إنترنت، وشيلُه ينقض ذلك.
   ولا حسابات: العضوية بتوكن جهاز لا بتسجيل دخول.
3. **الإجابة الخاطئة تنقل الخلية للفريق الآخر مباشرة** (القاعدتان الأخريان
   موجودتان كخيار في الإعدادات لكن هذه الافتراضية)
4. **بنك أسئلة كامل من البداية** — 280 سؤالاً جاهزة
5. **أونلاين: المضيف هو المقدّم والحَكَم** (2026-08-07). اللعبة أصلاً بلا
   إدخال إجابة — تُقال بصوت عالٍ — فالحكم بيد إنسان لا بيد مطابق نصوص.
   وصاحب الدور يختار الخلية من جهازه، والباقون يشاهدون.

---

## 3. الحالة الحالية — ما أُنجز ✅

### البنية
- ✅ خمسة ملفات JS بفصل صارم: `hive.js` منطق خالص، `game.js` قرارات،
  `ui.js` رسم، `setup.js` إعدادات، `utils.js` أدوات
- ✅ خمس شاشات: الرئيسية · كيف نلعب · الإعدادات · اللعب · النهاية
- ✅ الهوية مبنية على `واجهة لعبة تفاعلية/design_handoff_horoof_game`:
  **ستة ثيمات** في `js/themes.js`، خطوط Cairo + Almarai محزومة محلياً

### اللعب
- ✅ لوحة مسدسات بصفوف مُزاحة، الحجم ٤×٤ إلى ٧×٧
- ✅ **ترتيب الحروف عشوائي في كل جولة**
- ✅ كشف الفوز بـ BFS على إحداثيات محورية + إبراز مسار الفوز متحركاً
- ✅ أشرطة ملوّنة على الحواف الأربع تبيّن اتجاه كل فريق
- ✅ نافذة السؤال: الحرف، السؤال، تلميح، مؤقّت، كشف الإجابة، حكم صح/خطأ
- ✅ ثلاث قواعد للإجابة الخاطئة
- ✅ جولات متعددة مع إنهاء مبكر عند حسم الأغلبية
- ✅ **تبديل الاتجاهات كل جولة** لإلغاء أفضلية الاتجاه العمودي
- ✅ من يبدأ: قرعة في الجولة الأولى، ثم يبدأ المتأخّر في النتيجة
- ✅ اختصارات لوحة مفاتيح للمقدّم (مسافة / 1 / 2)
- ✅ مؤثرات صوتية مولّدة برمجياً (بلا ملفات صوت)

### الإعدادات
- ✅ **اختيار الثيم** من ستة، وتبديله يعيد ألوان الفريقين لافتراضياته
- ✅ أسماء الفريقين وألوانهما (8 ألوان لكل ثيم، ومنع اللونين المتطابقين)
- ✅ حجم الخلية، عدد الجولات، وقت السؤال، قاعدة الخطأ
- ✅ معاينة مصغّرة حيّة تُظهر أثر الحجم واللون قبل البدء
- ✅ الإعدادات تُحفظ في `localStorage` وتبقى بين الجلسات

### الأونلاين (2026-08-07)
- ✅ روم بكود من ست خانات، **حتى ثمانية لاعبين**، والمضيف يوزّعهم على الفريقين
- ✅ **المضيف = المقدّم**: يكشف الإجابة ويحكم وينقل الجولة ويبدأ المباراة
- ✅ **صاحب الدور يفتح الخلية من جهازه** — أي لاعب في الفريق صاحب الدور
- ✅ اللوحة والحروف والأسئلة **تُبثّ** فيراها الجميع نفسها (2.5 KB للجولة)
- ✅ المؤقّت ختم زمني مشترك (`deadline`)، والحكم عند انتهائه للمقدّم وحده
- ✅ رابط دعوة `?room=CODE` + نسخ ومشاركة، وعودة تلقائية بعد تحديث الصفحة
- ✅ إعدادات المضيف تحكم المباراة، وتُستعاد إعدادات اللاعب عند خروجه
- ✅ **مكتبة Supabase تُجلب عند الطلب** لا مع الصفحة — فاللعب المحلي يبقى
  بلا أي نداء شبكة
- ✅ `supabase-huroof.sql` **شُغِّل (2026-08-07)** وجُرّب حيّاً — راجع القسم 4

### بنك الأسئلة
- ✅ **280 سؤالاً، 10 لكل حرف من الـ28**
- ✅ متحقَّق آلياً: كل إجابة تبدأ بحرفها فعلاً، ولا سؤال مكرر
- ✅ لا يتكرر السؤال داخل المباراة الواحدة حتى تنفد أسئلة الحرف

---

## 4. ما جرى التحقق منه فعلياً (لا قراءة كود فقط)

| الاختبار | النتيجة |
|---|---|
| 200,000 لوحة عشوائية، مقاسات ٤–٧ | **صفر تعادل، صفر فوز مزدوج** |
| 540 مباراة كاملة عبر دوال اللعبة الحقيقية | **صفر مسار فوز خاطئ، صفر تعليق** |
| صحة كل مسار فوز (متّصل + يلمس الحافتين) | ✅ بمدقّق مستقل |
| مباراة ٣ جولات كاملة | ✅ التبديل والأغلبية والشاشة النهائية |
| نافذة السؤال والمؤقت | ✅ العرض والحكم والإيقاف |
| الجوال 375×812، كل المقاسات | ✅ بلا تجاوز أفقي، اللوحة كاملة بلا تمرير |
| الشاشة الكبيرة، كل المقاسات | ✅ |
| 280 إجابة مقابل حروفها | ✅ كلها مطابقة، صفر تكرار |
| الثيمات الستة × 24 متغيّراً | ✅ كلها تُحَل، وألوان الفرق تتبع الثيم |
| 150 مباراة بعد إعادة بناء الواجهة | ✅ 7,606 سؤال، صفر أخطاء |
| مقاس الخلية مقابل المواصفة على 1440×960 | ✅ 106 / 94 / 82 / 72 مطابقة |
| اللعبة والخادم مطفأ (تحقق بـ curl) | ✅ مباراة كاملة من المخزن |
| التمرير في كل الشاشات، 375×812 و1280×800 (2026-08-14) | ✅ الشاشة الفعّالة تمرّر داخلها، والصفحة لا تمرّر، والهيدر ثابت |
| اللوحة مع شريط الروم ظاهراً (2026-08-14) | ✅ 685/685 بلا تمرير زائد |
| لقطات App Store أُعيد توليدها (2026-08-14) | ✅ 8 لقطات، وقيست بـPIL: 1320×2868 و1242×2688 بالبكسل |
| أصول حزمة iOS قبل الرفع (2026-08-14) | ✅ أيقونة 1024 بنمط RGB بلا شفافية، والمعرّف واحد في `capacitor.config.json` و`project.pbxproj`، و`ITSAppUsesNonExemptEncryption=false` |

**التعادلات الوحيدة** ظهرت في قاعدة «الخلية تُقفل» (26 من 180)، وهي متوقعة
لأن القفل يحذف خلايا فتنكسر الضمانة الرياضية. اللعبة تتعامل معها بالفوز
لصاحب الخلايا الأكثر.

### الأونلاين على Supabase الحقيقية (2026-08-07)

تبويبان على **أصلين مختلفين** (`localhost` و`127.0.0.1`) ليكون لكلٍّ تخزينه
وتوكنه — أبسط طريقة لمحاكاة جهازين.

| الاختبار | النتيجة |
|---|---|
| إنشاء روم ودخول ثانٍ بالكود (`MR8QE2`) | ✅ والمضيف يراه فوراً بإشارة `roster` |
| التوزيع التلقائي على الفريق الأقلّ | ✅ الداخل صار في «الثاني» بلا تدخّل |
| المضيف ينقل لاعباً بين الفريقين | ✅ ويصل للضيف، وزر البدء يتبع الجاهزية |
| **الضيف يحاول نقل المضيف** | ✅ مرفوض `not_host` من قاعدة البيانات |
| بدء المباراة | ✅ نفس الـ16 حرفاً حرفياً على الجهازين |
| الضيف يفتح خلية | ✅ السؤال نفسه يظهر عند المقدّم |
| **المؤقّت المشترك** | ✅ 30 عند الفتح، و19 عند المقدّم بعد 11 ثانية |
| **الضيف يحاول الكشف والحكم** | ✅ لا شيء يتغيّر (الحارس في `game.js`) |
| جولة كاملة: 13 حكماً، 7 فتحات للمقدّم و6 للضيف | ✅ نفس المُلّاك والنتيجة ومسار الفوز |
| إعلان الجولة عند الضيف | ✅ بلا زر، وبـ«بانتظار المقدّم…» |
| شاشة النهاية عند الضيف | ✅ «الرئيسية» وحدها |
| **تحديث صفحة الضيف وسط الروم** | ✅ لافتة العودة ثم رجوعه لمقعده وفريقه وحالته |
| غريب يستعلم / يكتب / يدخل رومًا بدأت | ✅ `not_member` · `not_member` · `started` |
| غريب يكتب `wins: {A: 99}` | ✅ مرفوض، والحالة سليمة، ولا توكن في أي ردّ |
| كود وهمي · توكن قصير | ✅ `not_found` · `bad_token` |
| **خروج المقدّم** | ✅ الروم تُحذف، والضيف يخرج للرئيسية وتُمسح جلسته |
| استعادة إعدادات اللاعب بعد الخروج | ✅ |

---

## 5. أشياء مهمة يجب معرفتها قبل التعديل ⚠️

1. **`hive.js` لا يحوي أي تعامل مع RTL** وهذا مقصود. «من اليمين لليسار» و«من
   اليسار لليمين» شرط واحد رياضياً. لا تُدخل اتجاه العرض في منطق اللعبة.

2. **الإحداثيات محورية داخلياً** رغم أن العرض بصفوف مُزاحة. لا تحسب الجيران
   في الإحداثيات المُزاحة — تختلف حسب فردية الصف وهو مصدر أخطاء كلاسيكي.

3. **متغيرات الحالة `let` في نطاق السكربت** (`SETTINGS`, `BANK`, `Game`).
   `window.SETTINGS = x` لا يغيّرها. للاختبار من الكونسول استخدم `eval` غير
   المباشر: `const ev = eval; ev("SETTINGS.size = 6");`

4. **لا تُخمّن المساحة برقم ثابت في `computeHexWidth`.** الثوابت المخمَّنة
   سبّبت مرتين مشاكل حقيقية: تجاوزاً أفقياً، ثم أكثر من 100px فاضية أسفل
   اللوحة على الجوال. المساحة تُقاس الآن من التنسيق المطبَّق ومن موضع
   الحاوية وارتفاع التذييل الحقيقي.

4.1 **ارتفاع بطاقات الفرق يحكم حجم اللوحة.** حين كُدّس محتواها عمودياً
   ارتفعت إلى 154px فانكمشت الخلية من 94 إلى 48. أبقِها سطرين.

5. **ارفع `CACHE_VERSION` في `sw.js` بعد أي نشر.** عامل الخدمة يخزّن
   cache-first ويتجاهل `?v=` (بسبب `ignoreSearch: true`)، فبلا رفع الرقم
   يبقى اللاعبون على النسخة القديمة إلى الأبد. هذا عطّل التطوير مرتين:
   تعديلات مطبَّقة على القرص ولا تظهر في المتصفح.

6. **الاختبار**: `npm test` يشغّل `tools/test.js` في متصفح نظيف بلا كاش.
   استخدمه بدل الكونسول اليدوي — المتصفح يخبّئ الملفات فتُقرأ نسخة قديمة
   ويبدو التعديل بلا أثر. الطريقة اليدوية القديمة:
   ```bash
   python -m http.server 8210 --bind 127.0.0.1
   ```
   ثم التحقق من المتصفح بـ JavaScript (تشغيل مباريات كاملة برمجياً وقياس
   العناصر). **لا تكتفِ بقراءة الكود.**

7. **لا يوجد اختصار لإغلاق نافذة السؤال** عمداً — الإغلاق بلا حكم تهرّب.

8. **رسائل الـ commit بالإنجليزية**، وتذكر ما جرى التحقق منه فعلياً.

9. **المنع الحقيقي في `game.js` لا في `ui.js`.** إخفاء زر الحكم عن غير
   المقدّم تجميل — الحارس في `resolveAnswer` و`selectCell` نفسيهما. تم
   التحقق: استدعاء الدالتين من الكونسول بمقعد لاعب عادي لا يغيّر شيئاً.

10. **زر «الجولة التالية» يُغلق الإعلان قبل أن يُنادي `nextRound`.** فحتى
    مع حراسة الدالة كان الضيف يضغط فيختفي الإعلان عنده وتقف المباراة وهو
    يظنّها معطّلة. الزر نفسه يُخفى عن غير المقدّم — وهذا **سلوك لا شكل**.

11. **إعدادات المضيف تُكتب في `SETTINGS` مباشرة بلا حفظ** أثناء الأونلاين.
    السبب: كل مسار رسم في المشروع يقرأ منها، وتمريرها كنسخة موازية كان
    يعني إعادة كتابة `ui.js` كله. وعند الخروج من الروم تُستعاد إعدادات
    اللاعب من `localStorage` (`Online.restoreSettings`).

12. **مكتبة Supabase محزومة في `assets/vendor/` لا من CDN.** بند آبل 2.5.2
    يمنع أن ينزّل التطبيق كوداً تنفيذياً ويشغّله، وجلبها من jsDelivr وقت
    التشغيل خطر رفض. وهي تُحمَّل **عند أول ضغطة «أونلاين»** لا مع الصفحة:
    211 كيلوبايت لا داعي لتفسيرها في كل إقلاع لمن يلعب محلياً.
    ⚠️ لتحديثها: أعد تنزيل ملف UMD نفسه فوق `assets/vendor/supabase.js`.

13. **داخل التطبيق `location.origin` هو `capacitor://localhost`.** فرابط
    الدعوة المبني منه لا يفتح عند أحد، و`window.open` ينقل WKWebView نفسه
    إلى الوجهة فيخرج اللاعب من اللعبة بلا رجوع. الحلّ في `setup.js`:
    `isNativeApp()` تفرّق، فيُرسل عنوان الويب المنشور، ويُنسخ الرابط بدل
    فتح واتساب.

14. **`www/` ليست ملفات اللعبة — هي نسخة منها.** `npm run build` ينسخ،
    و`npx cap copy ios` يضعها في التطبيق. **من عدّل اللعبة ولم يُشغّلهما
    بنى تطبيقاً من ملفات الأمس بلا أي إنذار** — وقع فعلاً: حزمة التطبيق
    بقيت على نسخة ما قبل الأونلاين.
    ⚠️ وسكربت البناء يتحمّل قفل OneDrive الآن (يفرّغ الملفات ويعيد
    المحاولة بدل حذف المجلد) — المحاولة الواحدة كانت تفشل بـ WinError 5.

15. **الحالة المبثوثة تحوي الحروف والأسئلة نصّاً.** لا يمكن لكل جهاز أن
    يولّدها بنفسه: الترتيب عشوائي والسحب عشوائي، فلن يتطابقا. والمقاس
    2.5 KB لجولة 4×4 — مقبول للبثّ وللجدول.

16. **وعاء التمرير هو `.screen.active` لا الصفحة**، والقاعدة عامة عمداً
    (2026-08-14). كانت محصورة بـ`@media (display-mode: standalone)`
    فانقسم السلوك: المتصفح يمرّر الصفحة كلها فيبدو كل شيء سليماً، والتطبيق
    يطبّق `overflow: hidden` على الجسم بلا أن يمنح الشاشة ارتفاعاً محسوماً
    — فلا هي مرّرت ولا الصفحة، وانقصّ كل ما تحت الطيّة (2105px من شاشة
    الإعدادات، و230px من الرئيسية). عطلٌ لا يظهر إلا داخل التطبيق.
    **لا تُعِد حصر القاعدة بوسيط عرض** — أي فرق بين المتصفح والتطبيق يعني
    عطلاً لا يُكتشف إلا بعد البناء.
    ولنفس السبب: الشاشة تملأ ما تبقّى بـ`flex: 1` ولا يُحسب ارتفاعها من
    `100vh` ناقص الهيدر — ذاك الحساب كان يتجاوز بارتفاع شريط الروم كلما
    ظهر (البند 4 نفسه: لا تُخمّن المساحة برقم).

---

## 6. قائمة المهام 📋

### 🔴 قبل أول جلسة حقيقية

- [ ] **جلسة لعب بشرية كاملة** — كل الاختبارات حتى الآن آلية. لم يلعبها إنسان
  من البداية للنهاية. هذه أهم من أي ميزة إضافية.

### 🟠 مهم

- [ ] **توسيع بنك الأسئلة** — 10 أسئلة لكل حرف تكفي لمباراتين أو ثلاث قبل أن
  تبدأ الإعادة. الهدف المعقول 25–30 لكل حرف (≈800 سؤال).
- [x] **مستوى الأسئلة** — أُنجز 2026-07-31، البنك كله صار مستوى **متوسط**
  (`_meta.difficulty`). تم التحقق: 280 سؤالاً، 28 حرفاً، صفر إجابة لا تبدأ
  بحرفها، صفر تكرار.
- [ ] **تقسيم لمستويات** — سهل/متوسط/صعب يسمح
  بلعب العائلة والأطفال معاً.
- [x] **تبديل السؤال غير المُجاب** — أُنجز 2026-07-31. كل خلية تبقى متاحة
  بعد فتح سؤالها تأخذ سؤالاً جديداً (إلغاء، خطأ مع «تبقى متاحة»، انتهاء وقت).
- [ ] **زر «استبدل السؤال»** للمقدّم قبل الإجابة، حين يكون السؤال غير مناسب.

### 🟡 تحسينات ظاهرة

- [x] **شعار وأيقونة** — أُنجز 2026-07-31. `tools/make-icons.py` يولّد 11 مقاساً
  وشاشة بداية من تعريف واحد، ووسوم `og:` مضافة.
- [x] **تطبيق مواصفة التصميم** — أُنجز 2026-07-31. الواجهة أُعيد بناؤها على
  `واجهة لعبة تفاعلية/design_handoff_horoof_game`: ستة ثيمات، خطوط
  Cairo + Almarai، شريط تنقّل، وإطار أشرطة حول اللوحة.
- [x] **عمل بلا إنترنت** — أُنجز 2026-07-31. الخطوط محزومة محلياً و`sw.js`
  يخزّن كل شيء. تم التحقق والخادم مطفأ: مباراة كاملة لُعبت.
- [x] **رفع على GitHub** — أُنجز 2026-07-31، مستودع عام `RajaXDX/huroof-raja`
- [x] **تفعيل GitHub Pages** — **مفعّلة أصلاً** وتخدم على
  `rajaxdx.github.io/huroof-raja`. البند كان مكتوباً كمعلّق وهو ليس كذلك؛
  تم التحقق 2026-08-07: الصفحة ترجع 200 وتحمل وضع الأونلاين، وسجلّ
  «pages build and deployment» فيه بناءات ناجحة منذ 2026-08-05.
- [x] **تجهيز App Store** — أُنجز 2026-07-31. مشروع Capacitor iOS، سير
  GitHub Actions يبني على ماك سحابي (صاحب المشروع على ويندوز)، سياسة
  خصوصية، ونصوص المتجر. الدليل في `APPSTORE.md`.
- [x] **تهيئة الحزمة للأيفون** — أُنجز 2026-08-07. ثلاثة أشياء كانت
  ستُفسد أول بناء:
  - حزمة التطبيق كانت من 1 أغسطس **بلا الأونلاين إطلاقاً** — أُعيد بناؤها
  - مكتبة Supabase كانت تُجلب من CDN وقت التشغيل (خطر البند 2.5.2) —
    صارت محزومة محلياً
  - رابط الدعوة وزر المشاركة كانا يعتمدان على `location.origin`
    و`window.open` — وكلاهما لا يصلح داخل WKWebView
- [x] **أول بناء فعلي على ماك** — أُنجز 2026-08-07 (`iOS #2` على `af9691d`).
  بلا توقيع وبلا أسرار ومجاناً، و**نجح في 58 ثانية**: `npm ci` ثم
  `build-www.py` ثم `cap sync ios` ثم `xcodebuild` — وخطوات التوقيع
  والرفع تخطّت نفسها كما هو مقصود.
  فمشروع Xcode سليم والحزمة تُترجم فعلاً على جهاز آبل.

- [ ] **النشر على App Store** — ما بقي كله حساب وأوراق لا كود:
  1. اشترك في Apple Developer Program (99 دولاراً سنوياً)
  2. أنشئ التطبيق في App Store Connect بمعرّف `com.rajaxdx.huroof`
  3. أضف **الأسرار الثمانية** في `Settings → Secrets and variables → Actions`
     (القائمة في رأس `.github/workflows/ios.yml`)
  4. شغّل نفس السير بـ`upload = true`
  5. اللقطات جاهزة في `screenshots/` بالمقاسين الإلزاميين
  ⚠️ **رقم البناء تلقائي — لا تلمسه.** كان مكتوباً هنا أن ترفع الرقم في
  `package.json`، وهذا خطأ: `package.json` غير موصول ببناء iOS إطلاقاً.
  الرقم الذي ترفض آبل تكراره هو `CFBundleVersion`، ويضبطه السير برقم
  التشغيل (`github.run_number`) فلا يتكرر. أما رقم الإصدار الظاهر فهو
  `MARKETING_VERSION` في `ios/App/App.xcodeproj/project.pbxproj` (الآن
  `1.0`) ويُرفع يدوياً عند **إصدار جديد في المتجر** لا عند كل بناء.
- [ ] **صوت أفضل** — النغمات المولّدة وظيفية لكنها بسيطة
- [ ] **حفظ نتائج المباريات** محلياً (كم مباراة، من فاز أكثر)

### 🟢 لاحقاً

- [x] **وضع أونلاين** — أُنجز 2026-08-07. الفصل بين `game.js` و`ui.js`
  أثبت جدواه: `Game` هو ما يُزامَن فعلاً، وأُضيف `net.js` (نقل) و
  `online.js` (وصل وصلاحيات) بلا أن يتغيّر `hive.js` بحرف واحد.
  السكربت شُغِّل وجُرّبت الرومات حيّاً — راجع جدول القسم 4.
  - **باقٍ**: جلسة بشرية على جوالين حقيقيين (الاختبار كله بتبويبين)
- [ ] **وسائل مساعدة** مثل «تخطّي» أو «سؤال بديل» بحصة محدودة لكل فريق
- [ ] **أكثر من فريقين**

---

## 7. كيف تُحدَّث هذه الخطة

عند إنجاز أي بند:
1. غيّر `[ ]` إلى `[x]` وأضف: `— أُنجز 2026-XX-XX (commit abc1234)`
2. انقل ما استُجدّ من ملاحظات إلى القسم 3 أو 5
3. غيّر تاريخ «آخر تحديث» في أعلى الملف
4. أضف أي مهمة جديدة في مكانها حسب الأولوية
