# حروف مع رجا — تعليمات المشروع

> ## 📌 اقرأ [`PLAN.md`](PLAN.md) أولاً
> هو **المرجع الرسمي** لحالة المشروع وقائمة المهام المتبقية، ويُحدَّث مع كل إنجاز.
> و[`README.md`](README.md) فيه شرح القواعد والبنية والتشغيل.

**عند إنجاز أي مهمة: حدّث `PLAN.md`** (`[ ]` ← `[x]` مع التاريخ ورقم الـ commit).

## نظرة عامة

لعبة خلية الحروف (HTML/CSS/JS خام بدون مكتبات). وضعان: **محلي** على جهاز
واحد، و**أونلاين** بروم بكود حتى ثمانية لاعبين عبر Supabase.
مشروع منفصل تماماً عن «تحدي رجا» وإن شاركه الهوية اللونية ومشروع Supabase
(كل جداوله مسبوقة بـ`hr_`).

## قواعد العمل

1. **الفصل بين الملفات صارم ومقصود:**
   `hive.js` منطق خالص بلا DOM · `game.js` قرارات بلا رسم · `ui.js` رسم بلا قرارات ·
   `net.js` نقل بلا قواعد · `online.js` الوصل والصلاحيات.
   لا تخلطها.

2. **لا تُدخل RTL في منطق اللعبة.** اتجاه العرض شأن `ui.js` وحده.

3. **شغّل اللعبة وتحقق فعلياً** بعد أي تعديل — لا تكتفِ بقراءة الكود:
   ```bash
   python -m http.server 8210 --bind 127.0.0.1
   ```

4. **ارفع `?v=` في `index.html`** بعد أي تعديل CSS/JS.

5. **رسائل الـ commit بالإنجليزية**، وتذكر ما جرى التحقق منه.

6. راجع **القسم 5 في `PLAN.md`** («أشياء مهمة قبل التعديل») قبل أي شغل —
   فيه مصائد اكتُشفت بصعوبة.
